deploy:
  env:
    - foo="bar"
    - another="hello world"
    - VAR_TF_MASTER_USER_NAME=notsecure
    - VAR_TF_MASTER_PASSWORD=needsecureENVvariables

  before_prov:
    - echo 'this is happening before provisioning'
    - ls -ltra
    - echo "value for foo is $foo"
    - echo "$another"

  on_prov_error:
    - echo 'provisioning failed miserably'
    - echo $foo

  on_prov_success: echo 'yay, it worked!'

  infra:
    - terraform: config
